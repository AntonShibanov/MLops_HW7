stages:
  - build
  - reproduce

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

cache:
  paths:
    - .cache/pip
    - venv/

before_script:
  - python -m venv venv
  - source venv/bin/activate
  - pip install --upgrade pip
  - pip install -r requirements.txt

build_model:
  stage: build
  script:
    - echo "Запуск ML-пайплайна..."
    - python ml_pipeline.py
  artifacts:
    paths:
      - model.pkl
      - hyperparameters.json
      - requirements.txt
    expire_in: 7 days

make_reproducible:
  stage: reproduce
  script:
    - echo "Проверка воспроизводимости..."
    # Проверим, что артефакты с предыдущего этапа доступны
    - ls -la model.pkl hyperparameters.json requirements.txt
    # Можно загрузить модель и прогнать инференс для демонстрации
    - python -c "import pickle, json; model=pickle.load(open('model.pkl','rb'));
      with open('hyperparameters.json') as f: params=json.load(f);
      print(f'Воспроизведена модель с параметрами {params}')"
